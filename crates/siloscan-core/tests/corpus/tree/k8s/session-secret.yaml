apiVersion: v1
kind: Secret
metadata:
  name: session-keys
  namespace: web
  annotations:
    kubernetes.io/description: session signing material for the storefront
type: Opaque
stringData:
  session-secret: "{{HEX_64_406}}"
  csrf-token: "{{B64URL_43_407}}"
  oauth-client-secret: "{{HEX_40_408}}"
